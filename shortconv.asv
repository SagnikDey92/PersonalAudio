function [ res ] = shortconv( x, h, M,N )
%UNTITLED Summary of this function goes here
%   Detailed explanation goes here 
    size_x = size(x, 1);
    I = size(h, 1);
    res = zeros(M, 1);
    x=[zeros(I-1,1) x];
    for n = N:-1:N-M+1
        for j = 1:I
            res(n-i+1, 1) = res(n-i+1, 1) + h(j, 1)*x(i-j, 1);
        end
    end
    res = res';
end

